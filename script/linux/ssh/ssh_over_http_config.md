Add the following entries into the `~/.ssh/config` configuration file:

**GitHub**

```
Host github.com
	Hostname ssh.github.com
	Port 443
```

**BitBucket**

```
Host bitbucket.org
	Hostname altssh.bitbucket.org
	Port 443
```
